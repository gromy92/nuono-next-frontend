# GitHub Management Pilot

This repository is part of the `nuono-next` GitHub management migration.

Current pilot scope:

- GitHub Issues track active delivery slices.
- GitHub Actions runs frontend install, typecheck, and build verification.
- Gitee remains the `origin` remote during the transition.
- GitHub is available as the `github` remote for PR and CI validation.

Workspace-level context still lives in `/Users/gromy/Documents/code/nuono/docs`.
